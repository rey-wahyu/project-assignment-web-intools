import { css } from "react-emotion";

export const cssPageTitleContainer = css({
  display: "flex",
  justifyContent: "space-between",
  marginBottom: 32,
  marginTop: 40,
});

export const cssTypo = css({
  color: "#B9B9B9",
});

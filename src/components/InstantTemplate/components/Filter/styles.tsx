import { css } from "react-emotion";

export const cssWidgetHeader = css({
  display: "flex",
  alignItems: "center",
  marginBottom: 16,
  button: {
    marginRight: 16,
  },
});

export const cssFlex = css({
  display: "flex",
});

export const gridContainer = css({
  alignItems: "center",
});
